export interface GoogleUserProfile {
  id: string;
  name: {
    familyName: string;
    givenName: string;
  };
  emails: Array<{
    value: string;
    verified: boolean;
  }>;
  photos: Array<{
    value: string;
  }>;
}

export interface GoogleUser {
  profile: GoogleUserProfile;
}
